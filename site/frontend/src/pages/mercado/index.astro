---
import BaseLayout from '../../layouts/BaseLayout.astro';
import { getCollection } from 'astro:content';

const posts = (await getCollection('mercado'))
  .sort((a, b) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf());
---

<BaseLayout title="Mercado">
  <div class="container secao-header">
    <div class="secao-icon">📡</div>
    <div>
      <h1>Mercado</h1>
      <p class="secao-desc">Lançamentos, parcerias e movimentos estratégicos de concorrentes nacionais e internacionais no setor de frotas.</p>
    </div>
  </div>

  <div class="container">
    {posts.length === 0 ? (
      <div class="vazio">
        <p>Nenhuma atualização ainda. O agente roda diariamente às 7h.</p>
      </div>
    ) : (
      <div class="posts-grid">
        {posts.map((post) => (
          <a href={`/mercado/${post.id}`} class="post-card">
            <div class="post-card-header">
              <div class="data">{post.data.pubDate.toLocaleDateString('pt-BR')}</div>
              {post.data.origem === 'brasil' ? (
                <span class="tag-br">🇧🇷 Brasil</span>
              ) : (
                <span class="tag-intl">🌍 Internacional</span>
              )}
            </div>
            <h3>{post.data.title}</h3>
            <p>{post.data.description}</p>
          </a>
        ))}
      </div>
    )}
  </div>
</BaseLayout>

<style>
  .secao-header {
    display: flex;
    align-items: flex-start;
    gap: 1.5rem;
    padding-top: 3rem;
    padding-bottom: 2rem;
    border-bottom: 1px solid var(--cinza-medio);
    margin-bottom: 2rem;
  }

  .secao-icon {
    font-size: 3rem;
    line-height: 1;
  }

  h1 {
    font-size: 2rem;
    margin-bottom: 0.5rem;
  }

  .secao-desc {
    color: var(--texto-suave);
    font-size: 1rem;
    line-height: 1.6;
    max-width: 600px;
  }

  .post-card-header {
    display: flex;
    align-items: center;
    justify-content: space-between;
    margin-bottom: 0.5rem;
  }

  .vazio {
    text-align: center;
    padding: 4rem 0;
    color: var(--texto-suave);
  }
</style>
